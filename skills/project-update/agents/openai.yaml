interface:
  display_name: "Project Update"
  short_description: "Keep company and project truth current"
  default_prompt: "Use $project-update to inspect current evidence, update COMPANY.md, PROJECT.md, .kernel/NOW.md, and only the company or project domain files whose durable truth actually changed."
policy:
  allow_implicit_invocation: true
