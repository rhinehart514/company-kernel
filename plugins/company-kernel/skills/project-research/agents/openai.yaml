interface:
  display_name: "Project Research"
  short_description: "Resolve a project-shaping uncertainty"
  default_prompt: "Use $project-research to resolve one uncertainty that could materially change the project model or decision while respecting the active system context."
policy:
  allow_implicit_invocation: false
