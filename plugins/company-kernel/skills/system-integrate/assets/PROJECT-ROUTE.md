For consequential work in this repository, apply the shared Company Kernel system at
`<SYSTEM_HOME>/SYSTEM.md`, then read `PROJECT.md` and only the linked `.project/*.md`
files relevant to the task. Existing implementation, safety, permission, and deployment
rules remain authoritative. Use `$project-context` when project truth moves and
`$project-research` for one uncertainty that can change it.
