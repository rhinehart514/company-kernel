For consequential project work, apply `SYSTEM.md` and only the linked `.system/*.md` lenses relevant to the task; then read `PROJECT.md` and only the linked `.project/*.md` files relevant to the work. These files provide project judgment and current truth; existing implementation, safety, permission, and deployment rules remain authoritative. Use `$system-integrate` when the model, rules, skills, tools, or system context change; use `$project-context` when project truth moves; use `$project-research` for a bounded uncertainty that can change it.
