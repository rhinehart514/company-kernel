interface:
  display_name: "Project Context"
  short_description: "Compile or refresh adaptive project context"
  default_prompt: "Use $project-context to inspect this project and create or refresh the smallest context set that keeps it clear, coherent, and current."
policy:
  allow_implicit_invocation: false
