interface:
  display_name: "Project Context"
  short_description: "Compile project context beneath the system layer"
  default_prompt: "Use $project-context to read the active system context, inspect current project evidence, and create or refresh the smallest project context set that keeps the work clear, coherent, and current."
policy:
  allow_implicit_invocation: false
