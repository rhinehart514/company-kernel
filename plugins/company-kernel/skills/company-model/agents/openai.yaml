interface:
  display_name: "Company Model"
  short_description: "Create or reconcile shared company state"
  default_prompt: "Use $company-model to create or reconcile the project's canonical COMPANY.md without inventing missing company truth."
policy:
  allow_implicit_invocation: false
