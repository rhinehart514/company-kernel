# Product coherence

A useful capability belongs only when it makes the product more obviously itself.
Increase capability without increasing the user's mental model.
Keep one clear promise across discovery, onboarding, product behavior, delivery, and proof.
Remove surfaces that expose internal machinery instead of advancing the core loop.
