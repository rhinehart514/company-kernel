# Company Kernel

For consequential work, identify the relevant domains: Coding, Product, GTM, or a combination.
Read only the matching files under `standards/`, then read `PROJECT.md`, `.kernel/NOW.md`, and matching project models that exist.
Use system files as reusable judgment and project files as current truth; do not collapse or repeat them.
Execute against current reality. Repository behavior, users, buyers, and observed outcomes outrank stale context.
Update project models only when evidence materially changes durable truth, intent, constraints, bets, or direction.
Do not load `reviews/` during normal work. Invoke `$kernel-review` only for a qualified signal or explicit human request.
Read `CAPABILITIES.md` only when skill, tool, data, or access routing matters.
